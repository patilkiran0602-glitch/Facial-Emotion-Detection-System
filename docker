FROM python:3.10-stin-buster (last pushed 2 years ago)

MORKDIR /app

COPY/app

RUN apt update -y && apt install awscli -y

RUN apt-get update && apt-get install ffmpeg libsm6 libxexté unzip y && pip install CHD ["python3", "app.py"]